{
  "name": "jenkins-pipeline-course",
  "version": "1.0.0",
  "description": "",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest --ci --coverage",
    "test:unit": "jest --ci --testPathPatterns=unit",
    "test:integration": "jest --ci --testPathPatterns=integration",
    "build": "mkdir -p dist && cp -r src/* dist/ && echo '{\"version\":\"'$npm_package_version'\",\"built\":\"'$(date)'\"}' > dist/build-info.json",
    "lint": "npx eslint src/ --ext .js"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.2.1",
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}
